# /uat — project overlay notes

## Skill improvement log
- 2026-09-05 · L2 drives the persistent profile `uat/.profile` (IndexedDB is the only DB; nothing clears it). `innerText` returns CSS-uppercased text — match header pills case-insensitively. Deck cards and beat tiles TOGGLE: never re-click a picked one. A decided project reopens Research on the EXPERT face (computed default) — don't assume the guided face on a return visit. Dialog radios are visually hidden; click the label.
