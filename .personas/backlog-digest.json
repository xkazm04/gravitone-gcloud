{
  "backlog_cap": 300,
  "generated_at": "2026-09-06T11:53:52.220914700+00:00",
  "pending_count": 43,
  "titles": {
    "accepted": [],
    "pending": [
      "The board counts descoped, liked, deepen and wounded but can filter by none of them",
      "A load-bearing claim at low confidence is never auto-queued for follow-up",
      "The music provenance record holds two of the five facts the standard names",
      "Both spend ceilings check without reserving, so concurrent calls can walk straight through them",
      "The registry map ranks this context's governing subject third, behind two that do not apply",
      "judgeProof can still void the basis of a lock, and only a view stops it",
      "Deep scan recommended: security-auditor on text-providers",
      "The engine's availability oracle asks one question the router does not",
      "The ladder has no seam for a fake provider, so a SUCCESSFUL serve cannot be tested",
      "Deep scan recommended: test-strategist on golden-path-tests",
      "The object-URL ratchet proves co-presence, not ownership",
      "The map gate globs three directories; the map covers five",
      "The CLI sandbox's platform half never runs on the machine that gates merges",
      "Twenty high-consequence modules are imported by no probe",
      "The repo's declared LLM chokepoint has no hermetic probe",
      "Deep scan recommended: accessibility-checker on foundry-ui",
      "lib/usePolling.ts belongs to no context, so no sweep will ever reach it",
      "Nothing in this repo can see a floating promise",
      "The 4s poll starts a new request whether or not the last one came back",
      "Verdict saves have no ordering guard, so a cleared verdict can come back",
      "The cull surface is invisible to assistive technology, on the page whose header says keyboard is the point",
      "Deep scan recommended: test-strategist on foundry-pipeline",
      "Deep scan recommended: risk-assessor on foundry-pipeline",
      "The map gate added this morning does not glob the directory this round swept",
      "The retry that makes a rename atomic is now written out twice",
      "The reader in lib/foundry closes the last 7% of the torn-catalogue window, and turns it into a 500",
      "A run whose generation aborted still reports `done`",
      "A resumed run loses the prompt and workflow of every candidate it resumes",
      "The Python half of this repo has no automated verification of any kind",
      "Deep scan recommended: test-strategist on foundry-api",
      "Deep scan recommended: risk-assessor on foundry-api",
      "An off-vocabulary readback is now scored honestly but still nowhere recorded",
      "Both run listings read two files per run sequentially, next to a sibling that does not",
      "Two extract runs created in the same second with the same slug can take the same id",
      "The foundry disk layer cannot be tested without writing to the operator's real catalogue",
      "Neither commit path is idempotent, so a crash mid-commit double-applies on retry",
      "Five writers share the style catalogue with no exclusion of any kind",
      "A load-bearing claim at low confidence is never auto-queued for follow-up",
      "The triage board counts descoped, liked, deepen and wounded but can filter by none of them",
      "Four more Fact fields are stalled at 1 of 21, and only sources[] is ratcheted",
      "A re-route ships one dialect's prompt verbatim to a different model class",
      "No frame element binds to a fact, so an arrow can assert a relation nothing sourced",
      "EditPlan.unchanged is required of the model, described to it nowhere, and read by nothing"
    ],
    "rejected": [
      "ShotSheet re-derives the whole sheet on every parent render",
      "Canvas layers can be moved only with a pointer",
      "A proposed prompt can be read only through a native title tooltip",
      "Both ledgers are CSS grids with no table semantics",
      "The review and the direction pass still key beats by a repeatable timecode",
      "Deep scan recommended: bounty-hunter on imaging-providers",
      "A Google edit is still booked at the 1K generate rate",
      "The browser client cannot send seed, which the route and every adapter honour",
      "Vendor content types are asserted onto ImageRef's mime union rather than checked",
      "Two adapters disagree about what an empty vendor answer means",
      "google.ts captures its env at module scope, against env.ts's written lazy-read decision",
      "lib/imaging/budget.ts belongs to no context in context-map.json",
      "The spend ceiling is per-process, so autoscaling multiplies it by the instance count",
      "The reroute chain's worst case outlasts the route's own maxDuration, and cleanup never runs",
      "A single failed candidate discards the whole paid batch"
    ]
  },
  "version": 1
}