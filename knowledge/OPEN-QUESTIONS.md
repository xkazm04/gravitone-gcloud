# Open questions

The evidence contract in `README.md` says a claim without a label is a preference. **ASSUMED is the
one label that owes a debt** — it means we reasoned without witnesses, and an assumption nobody wrote
down becomes a fact by attrition. This file is where those debts live until a corpus pass discharges
them.

One entry per open question. Kill an entry by *answering* it — with the evidence label the answer
earns — never by deleting it quietly.

---

## Q1 · Is the hazard list in `ENGINES.md` complete, or merely un-probed?

**Raised:** 2026-08-12, by the `/gauntlet` run-1 adoption pass (`ENGINES.md` § Hazard).
**Label on the claim it supports:** ASSUMED.

Five of seven engines gained a hazard line — C, E, F, B, D — each traced to a specific L1 seat that
hit it. **A and G have no hazard line, and we do not know whether that is because they are safe or
because nobody in the cast ran a topic that would have exposed them.**

The distinction matters because a hazard axis with silent entries reads as a clean bill of health.
Five hazards came from twenty topics; the sampling was not designed to probe hazard at all — it fell
out of seats reasoning about their own exposure.

**What would discharge this:**
- A deliberate pass over A (Reversal Chain) and G (Paradox Teaser) asking *what does a wrong render
  cost on this engine* — the question `bill-analysis` said the catalogue never asks, and the one
  with the widest blast radius outside the schema.
- Specifically for **A**: it is the engine that turns on the viewer's own prior being wrong. On a
  subject where the prior is held by an identifiable group rather than "everyone", the reveal has a
  target. No seat tested that.
- Specifically for **G**: short-form derived from a longer notebook. The hazard candidate is that
  compression drops the qualifier — which is already *demonstrated* elsewhere (`consumer-scam`
  found "in risk-on conditions" and "(~2% of supply)" present in the notebook and absent from the
  render), so G may inherit a known defect rather than have one of its own.

**Until discharged:** treat an absent hazard line as *unknown*, not as *none*.

---

## Q2 · Does `TONE.md`'s "not a dial" list hold at the render boundary?

**Raised:** 2026-08-12, from `/gauntlet` run 1 philosophy finding P4.
**Label:** OBSERVED that it failed once; ASSUMED that the failure generalises.

`TONE.md:47-49` rules hedging density "not a dial" and then stops — there is no gate. The Bitcoin
run measured a tone profile stripping **5 of 9 hedges, 56% of the script's epistemic marking**, from
an already-approved chain with no dial set, because rate × duration is a word budget and hedges are
the cheapest words. The fix was drafted in that run's own `TONE-TEST.md:287-297` and never adopted.

**What would discharge this:** run the same measurement across the other tone profiles and both
remaining engines. If the strip rate tracks the word budget rather than the profile, the finding is
mechanical and the gate belongs in the render check — not in `TONE.md`'s prose.
